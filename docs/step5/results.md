# step5 결과 — 지침 신호도 내용에 실린다 (RQ3 인과)

> **스텝:** step5 · **RQ:** RQ3 (인과) · **결과:** `results/step5_instr-cause/` 336개
> **재현:** `python scripts/step5_reaggregate.py results/step5_instr-cause --figs docs/step5/figures`

---

## 1. 무엇을 어떻게 쟀나

지침 규칙문의 **지시어 단어**(`camelCase`)에 대해 모델이 남긴 내부 표현을
**반대 지침(`snake_case`)의 것으로 바꿔치기**하고, 행동이 반대 지침 쪽으로 넘어가는지를 잰다.
글자는 그대로 두고 내부 표현만 바꾼다.

- **넘어감(전이율) = (S_개입 − S_기준) / (S_반대지침 − S_기준)**,
  **S = logP(지침 목표 표기 이름) − logP(반대 표기 이름)**.
  목표 표기가 조건마다 다르므로 이 정의를 그대로 써야 두 방향을 함께 다룰 수 있다.
- 바꾸는 방식 3가지: 어텐션만 / 내용만 / 둘다. 층은 전 층 스윕.
- 모델 4개 × 묶음 42개 × 지침 방향 2종 = 336개. 시드 42, 평균 덮어쓰기.
- 바꾸는 대상은 **규칙문 지시어의 첫 등장 하나**다(후보열거의 같은 단어는 그대로 둔다).
- 지침이 행동을 흔들지 못한 조건(|S_반대 − S_기준| < 1.0)은 **판정 불가로 분리**한다.

---

## 2. 결과

![방향별](figures/direction_qwen.png)
![방향별](figures/direction_stability.png)

**봉우리 층, 지침 방향별**

| 모델 | 봉우리 | camel 방향 | snake 방향 | 어텐션만 | Key/Value | 판정불가 |
|---|---|---|---|---|---|---|
| Qwen2.5-Coder-3B | L27 | 0.85 | 0.70 | 0.09~0.13 | **0.12~0.15** | 0/84 |
| DeepSeek-Coder-6.7B | L17 | 0.41 | 0.42 | 0.17~0.18 | 0.41~0.43 | 0/84 |
| StableCode-3B | L19 | **0.45** | **1.32** | 0.18~0.28 | 0.21~0.40 | 0/84 |
| Llama-3.2-3B | L24 | 0.02 | 0.02 | 0.01 | — | **50/84** |

**StableCode는 방향에 따라 3배 차이가 난다.** 두 방향을 합친 평균은 0.89지만 중앙값은 0.76이고,
84개 중 38개가 전이율 1을 넘는다. **이 모델은 크기를 보고하지 않고 층과 Key/Value 대비만 쓴다.**

**판정 기준선 민감도** — 기준을 0.5 / 1.0 / 2.0 어디에 두어도 전이율은 바뀌지 않는다.
Llama는 남는 조건이 78 → 34 → 2로 크게 줄지만 값은 세 기준 모두 0.02다.

---

## 3. 해석

**(1) 지침 신호도 내용(Value)에 실린다.**
내용만 바꾸면 행동이 반대 지침 쪽으로 넘어간다. 어텐션만 바꾸면 거의 넘어가지 않는다.
Key/Value 비는 Qwen 0.12~0.15, StableCode 0.21~0.40으로, **방향을 나눠도 어텐션이 크게 뒤진다.**
코드 신호(step3)와 **같은 통로**다.

**(2) 지침이 작동하는 층은 지침을 보는 층과 같다.**
Qwen L27, DeepSeek L17, StableCode L19 — 세 모델 모두 step4의 관측 봉우리와 일치한다.
**지침이 안 먹히는 이유는 층이 어긋나서가 아니다.**

**(3) 범용 모델에는 지침 레버가 없다.**
Llama는 반대 지침을 실제로 주어도 점수가 거의 안 움직인다(84개 중 50개가 기준선 미만).
살아남은 조건에서도 전이율 0.02다. **"지침이 안 통하는" 것이 아니라 "지침 축 자체가 약하다".**

---

## 4. 이 스텝이 다루지 않는 것

- **전이율의 크기는 상한이다.** 바꿔치기는 원래 표현을 지우는 효과를 함께 내며,
  이 데이터에는 그 몫을 분리할 통제 조건이 없다.
- **규칙문 지시어 한 곳만** 바꾼다. 후보열거에 같은 단어가 남아 있어 효과가 희석될 수 있다.
- 긍정형·약한 어조 고정. 측정 대상은 고정된 함수 이름 한 쌍이다.
- step3(코드)와 봉우리 층을 직접 비교하지 않는다 — step3는 camel 한 방향,
  step5는 양방향이라 조건 집합이 다르다.
