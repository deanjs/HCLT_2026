# 진단 A 방법론 — 평균 덮어쓰기가 Key를 망가뜨리는가

> **성격:** 실험이 아니라 **방법 점검**. step3·step5가 공유하는 치환 장치를 검사한다.
> **결과:** `docs/diag/results.md`
> **코드 해설:** `code.md` · **코드 배경:** `../코드_하네스공통.md`

---

## 1. 왜 필요했나

step3·step5의 결론은 **"어텐션(Key) 경로는 표기 정보를 나르지 않는다"** 이다.
그런데 우리 방법이 **Key만 불리하게** 만들었을 가능성이 있었다.

```
   K_p = 회전(p) · (원래 키)      ← 위치가 회전 형태로 박혀 캐시에 들어간다
   V_p = (원래 값)                ← 위치 정보 없음
```

우리는 **여러 위치의 조각을 평균**낸다. 회전 방향이 엇갈리면 서로 지워져
**원래보다 짧은 벡터**가 들어간다. Value에는 이 문제가 없다.

→ 편향의 방향이 **우리 결론과 같다.** 독립 감사 문서 3편이 모두 이 지점을 지적했다.

---

## 2. 무엇을 재나

```
             ‖ (1/n) Σ_p k_p ‖
   줄어듦 = ────────────────────           1이면 손해 없음
             (1/n) Σ_p ‖k_p‖
```

**Key만 재면 판단할 수 없다.** 서로 다른 조각을 평균 내면 회전이 없어도 조금은 줄어든다.
그래서 **Value의 같은 값을 기준선으로 함께 잰다.**

| 결과 | 뜻 |
|---|---|
| Key 줄어듦 ≈ Value 줄어듦 | 회전 탓 추가 손해 없음 → **결론 확정** |
| Key 줄어듦 ≪ Value 줄어듦 | 우리가 Key를 망가뜨렸다 → 위상 보정 후 재측정 |

**함께 기록하는 것**

- 조각들 사이 평균 코사인 (방향이 얼마나 흩어졌나)
- **덮을 자리와 공여 자리의 위치 차이** (어긋날수록 회전 위상도 어긋난다)

---

## 2-1. 프롬프트는 새로 만들지 않는다 — step3·step5 것을 그대로 쓴다

**이 진단은 새 실험이 아니다.** step3(코드 대상)·step5(지침 대상)와 **완전히 같은 조건 객체**를
넣고 `mode`만 `kv_diagnose`로 바꾼 것이다. 그래서 프롬프트도 그대로다.

| 대상 | 어느 프롬프트 | 덮을 자리 | 떠올 자리 |
|---|---|---|---|
| `intervention_target="code"` | step3의 ①②③ (→ [`../step3/방법론.md`](../step3/방법론.md) §2-1) | 위반 이름 6개 | 공여 이름 6개 |
| `intervention_target="instruction"` | step5의 ①② (→ [`../step5/방법론.md`](../step5/방법론.md) §2-1) | 규칙문 지시어 1개 | 반대 지침의 지시어 1개 |

**같은 입력을 쓰는 것이 이 진단의 전부다.** 다른 조건에서 재면 "그 실험에서 Key가
불리했나"라는 질문에 답하지 못한다.

---

## 2-2. 측정 절차 — 점수를 안 매긴다

```
   1. step3·step5와 **같은 함수**(_preference_context)로 캐시와 정렬을 얻는다
        → 어느 자리를 덮고 어느 자리에서 떠올지가 그 실험과 글자 하나 안 틀리고 같다

   2. 층 L, 이름 하나마다:
        공여 조각들의 K 벡터 n개를 꺼낸다   (Qwen 코드 대상이면 이름당 2개)
        · 분자 = 그 n개를 평균 낸 벡터의 길이   ‖ (1/n) Σ k_p ‖
        · 분모 = 개별 길이의 평균              (1/n) Σ ‖k_p‖
        · 줄어듦 = 분자 ÷ 분모                  1이면 손해 없음

   3. 같은 것을 V로도 잰다  ← **이게 기준선이다**
   4. 조각들 사이의 평균 코사인도 함께 낸다 (방향이 얼마나 흩어졌나)
   5. 덮을 자리와 떠올 자리의 위치 차이도 기록한다 (어긋날수록 회전 위상이 어긋난다)

   ※ 덮지 않는다. 점수도 안 낸다. 캐시에서 벡터를 꺼내 산수만 한다
```

**왜 Value가 기준선인가.** 서로 다른 조각을 평균 내면 **회전이 없어도** 길이는 줄어든다.
그래서 Key만 재면 "0.95가 나쁜 값인가"를 판단할 수 없다. Value는 위치 회전(RoPE)이
안 걸리는 경로라, **회전을 뺀 순수한 평균 손실**이 얼마인지 알려준다.

**Qwen L25 · 코드 대상 실측:**

| | 줄어듦 | 조각 코사인 |
|---|---|---|
| Key (회전 있음) | **0.969** | 0.881 |
| Value (회전 없음) | 0.858 | 0.471 |
| 차이 (K−V) | **+0.111** | |

**Key가 오히려 덜 줄어든다.** 우려했던 방향(Key ≪ Value)의 정반대다.
같은 파일의 `position_offsets`는 전부 `0.0` — 덮을 자리와 떠올 자리가 **정확히 겹쳤다**.

**조건 수.** 4모델 × 6묶음 × 3갈래 = **72개**. 점수를 안 매기므로 **모델 하나에 몇 분**이다.

---

## 2-3. 직접 찍어 보기 (GPU 불필요)

```bash
python3 -c "
import json, glob
f = sorted(glob.glob('results/diag_kv-phase/*qwen*'))[0]
r = json.load(open(f)); ex = r['metrics']['extra']
print('대상:', ex['intervention_target'], '| 공여:', ex['donor'])
print('덮을 이름:', ex['viol_names'])
print('떠올 이름:', ex['donor_names'])
print('조각 수  : 덮을', ex['target_piece_counts'], '떠올', ex['donor_piece_counts'])
print('위치 차이:', ex['position_offsets'], '→ 평균', ex['position_offset_abs_mean'], '칸')
print()
v = r['metrics']['per_layer']['25']
print('L25  Key 줄어듦', round(v['key_shrink'], 3), '| Value 줄어듦', round(v['value_shrink'], 3),
      '| 차이', round(v['key_minus_value'], 3))
print('     Key 코사인', round(v['key_piece_cosine'], 3), '| Value 코사인', round(v['value_piece_cosine'], 3))
"
```

`*qwen*` 을 `*deepseek*` 등으로 바꾸면 다른 모델이 나온다. DeepSeek·StableCode는
`position_offsets`가 0이 아닌 것(3칸 수준)을 확인할 수 있다.

---

## 3. 왜 가벼운가

**점수를 매기지 않는다.** 프롬프트를 몇 번 통과시켜 캐시만 얻으면, 나머지는 산수다.
전 층 스윕(층수 × 3방식 × 채점)에 비해 훨씬 싸다 — **모델 하나에 몇 분.**

```
Condition (step3·step5와 완전히 같은 입력)
   │
   ▼
run(condition, mode="kv_diagnose")
   │
   ▼
handle.kv_substitution_diagnostics()
   │
   ├─ _preference_context()          치환 경로와 **같은 함수**로 캐시·정렬 확보
   └─ for 층:  공여 조각들의 K와 V 각각에 대해 줄어듦·코사인 계산
   │
   ▼
Metrics.per_layer[층]{key_shrink, value_shrink, key_minus_value, *_piece_cosine}
```

**같은 입력을 쓰는 것이 중요하다.** 다른 조건에서 재면 진단이 무의미하다.

---

## 4. 결과 JSON 읽는 법

> 필드 요약만 여기 둔다. **예시 JSON 전문과 함정은 [`code.md`](code.md)**.

`results/diag_kv-phase/*.json` — 4모델 × 6묶음 × 3갈래 = 72개

| 필드 | 뜻 |
|---|---|
| `per_layer["<층>"]["key_shrink"]` | Key 줄어듦 (1이면 손해 없음) |
| `..."value_shrink"` | Value 줄어듦 (**기준선**) |
| `..."key_minus_value"` | 차이. **음수가 크면 Key만 손해** |
| `..."key_piece_cosine"` / `"value_piece_cosine"` | 조각들이 같은 방향을 보나 |
| `extra.position_offset_abs_mean` | 덮을 자리 ↔ 공여 자리 위치 차이(칸) |
| `extra.donor_piece_counts` / `target_piece_counts` | 조각 수 |
| `extra.intervention_target` | `code`(step3) / `instruction`(step5) |

---

## 5. 결과 — 우려는 기각됐다

| 모델 | 대상 | Key 줄어듦 | Value 줄어듦 | 차이 |
|---|---|---|---|---|
| Qwen | 코드 | 0.948 | 0.814 | **+0.134** |
| | 지침 | 0.963 | 0.892 | **+0.071** |
| DeepSeek | 코드 | 0.957 | 0.785 | **+0.172** |
| Llama | 코드 | 0.948 | 0.810 | **+0.138** |
| StableCode | 코드 | 0.955 | 0.785 | **+0.170** |

**Key가 오히려 덜 줄어든다.** 8개 비교 전부 양수.

**이유:** 평균 내는 대상이 **한 단어 안의 이웃한 조각 2~3개**다. 회전은 위치 차이에 비례하는데
2~3칸으로는 위상이 거의 안 벌어진다. Key 조각 코사인 0.80~0.86 vs Value 0.26~0.59.

**위치 어긋남:** Qwen·Llama는 **0칸**(camel/snake 토큰 수가 같아 뒤가 안 밀린다).
DeepSeek·StableCode는 3칸, 지침 쪽은 0.5칸.

---

## 6. 이 진단이 확정한 것

1. **"어텐션 경로는 표기 정보를 나르지 않는다"를 그대로 주장할 수 있다.**
   리뷰어가 가장 먼저 찌를 지점이 막혔다.

2. **"어텐션을 얹으면 신호가 깎인다"(step3 §9)도 방법의 산물이 아니다.**
   Qwen은 위치 어긋남 0칸·Key 줄어듦 0.948로 멀쩡한데도 같은 현상이 나온다.
   → 어텐션과 내용이 **상호작용한다**는 실제 현상이다.

3. **위상 보정 재측정(실험 C)은 취소.** 보정할 왜곡이 없다.

---

## 7. 한계

- **줄어듦(노름 비)만 본다.** 방향이 얼마나 틀어졌는지는 코사인으로만 간접 확인한다
- 6묶음만 본다(값이 묶음마다 크게 안 변한다는 전제)
- 이 진단은 "Key가 망가졌나"만 답한다. 전이율의 **크기**는 별개 문제다(그건 통제로 해결)
