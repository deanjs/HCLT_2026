# 논문 골격 — HCLT 2026 (6쪽)

> **목적:** 본문을 쓰기 전에 **어느 절에 무엇을 넣고 무엇을 주장할지**를 먼저 못 박는다.
> 그래야 "가진 결과를 다 넣으려다 6쪽을 넘기는" 사고를 막는다.
> **템플릿 규정:** [`논문작성_유의사항.md`](논문작성_유의사항.md) · **결과 원본:** `docs/step<N>/results.md`
>
> ⚠️ 이 문서는 **제안**이다. §7의 결정 사항에 답을 받은 뒤 본문 집필에 들어간다.

---

## 0. 한 문장 요약과 기여

**한 문장:** 코드 생성 모델이 표기 규약 지침을 어기는 것은 지침을 **덜 봐서가 아니라**,
문맥의 표기 신호와 지침의 영향력이 모두 **어텐션이 아닌 내용(Value)** 통로를 타고
**후반 특정 층**에 모이기 때문이며, 그 층의 내용을 밀면 준수가 되살아나지만
어텐션을 키우는 기존 접근은 실패한다. (모든 인과 주장은 **평균 덮어쓰기 KV 개입 규격**에서의 결과다 — §3-2)

**기여 3가지** (서론 마지막 단락에 그대로 들어간다)

| | 기여 | 근거 스텝 |
|---|---|---|
| **C1** | 문맥의 규약 위반이 준수율을 무너뜨리며, 완만한 하락이 아니라 **임계**임을 보인다 | step1 |
| **C2** | **통제를 뺀 순효과**로, 표기 신호와 지침 영향력이 모두 Key가 아닌 **Value**에 실리고 **후반 단일 층**에 국소화됨을 인과적으로 특정한다 | step2·3·4·5 |
| **C3** | 그 발견에서 동기를 얻은 **잔차 스트림 조향**이 어텐션 증폭보다 효과적임을 보이고, 동시에 **선호 점수 회복이 준수율을 대변하지 않음**을 밝힌다 | step6 |

C2의 "통제를 뺀"과 C3의 "점수≠준수율"이 **방법론적 기여**다. 둘 다 우리가 스스로
상한값을 발견하고 깎아낸 결과이므로, 리뷰어에게 신뢰의 근거가 된다.

---

## 1. 쪽수 배분 (6쪽 = 2단 · 10pt)

| 장 | 제목(가안) | 쪽 | 그림/표 |
|---|---|---|---|
| — | 제목·요약·주제어 | 0.35 | — |
| **1** | 서론 | 0.65 | — |
| **2** | 관련 연구 | 0.35 | — |
| **3** | 실험 설계 | 0.90 | 표1(조건 축) |
| **4** | 문맥 위반과 준수율 절벽 (RQ1) | 0.55 | **그림1** 절벽 |
| **5** | 신호는 어디에 있는가 (RQ2·RQ3) | 1.65 | **그림2·3**, 표2·3 |
| **6** | 처방: 잔차를 밀 것인가 어텐션을 키울 것인가 | 1.05 | **그림4**, 표4 |
| **7** | 논의·한계·결론 | 0.35 | — |
| — | 참고문헌 | 0.60 | — |
| | **합계** | **6.00** | 그림 4 · 표 4 |

**5장이 논문의 심장이다.** 여기에 가장 많은 쪽을 준다.
**6장은 5장의 예측을 검증하는 장**이지 새 실험 나열이 아니다 — 이 위계가 흐려지면 논문이 산만해진다.

---

## 2. 장별 설계

### 1장. 서론 (0.65쪽)

| 단락 | 내용 | 담을 것 |
|---|---|---|
| ① | **문제 제기** | 코드 LLM에 "camelCase로 써라"를 줘도 자주 어긴다. 실무에서 스타일 일관성은 리뷰 비용·자동화와 직결 |
| ② | **통념과 기존 접근** | "지침을 덜 봐서 어긴다" → 지침 스팬의 어텐션을 키우는 처방(Spotlight 계열) |
| ③ | **우리 반론** | 지침은 이미 충분히 읽힌다. 문제는 **무엇을 보느냐가 아니라 무엇이 실려 오느냐**다 |
| ④ | **기여 3가지** | §0의 C1·C2·C3 |

> **주장하지 말 것:** "어텐션은 무익하다"(범용 모델 Llama에서 Spotlight가 0.904로 작동한다).
> **"코드 특화 모델에서"** 라는 범위를 서론부터 붙인다.

---

### 2장. 관련 연구 (0.35쪽)

세 갈래만. 각 2~3문장.

| 갈래 | 무엇 | 우리와의 관계 |
|---|---|---|
| 지침 준수·제어 생성 | 지침을 안 지키는 현상 보고 | 우리는 **왜**를 모델 내부에서 답한다 |
| 기계적 해석 | 반사실 개입(activation patching), CAA·ITI 조향 | 우리는 그 도구를 **표기 규약**에 처음 적용하고, **통제를 필수화**한다 |
| 어텐션 조향 | Spotlight 등 어텐션 재가중 | 우리가 **재구현해서 반박**하는 대상 |

---

### 3장. 실험 설계 (0.90쪽)

여기서 한 번에 정의하고, 4~6장은 이 정의를 재사용만 한다.
**아래 수치는 `results/` 8,556개를 전수 조사해 확인한 값이다**(추정·기억 아님).

#### 3.1 자극 — 함수 이름 504개

| 항목 | 값 | 왜 이렇게 |
|---|---|---|
| 이름 풀 | 동사 50 × 명사 50에서 **504개** | 효과가 특정 단어의 우연이 아님을 보이려면 이름이 많아야 한다 |
| 묶음 | 12개씩 **42묶음** (중복 없이 504개 전부 커버) | 한 조건 = 한 묶음. 42개가 통계 단위가 된다 |
| 이름 형태 | 전부 **동사+명사 두 단어** | 한 단어면 camel과 snake가 같아져 판정 불가 |
| 함수 본문 | **전 항목 동일** (`return value`) | 함수 사이에서 변하는 것을 이름 표기로 한정 |
| 언어 | **파이썬만** | ← §3.7 주의 |
| 프롬프트 | system(지침) + user(코드 블록 + 함수 추가 요청) | |

지침 문장(고정, 긍정형·약한 어조):

```
You are helping extend an existing Python module.
In this project we generally write function names in camelCase.
Every function name uses one of two styles only: camelCase or snake_case.
```

둘째 줄이 **규칙문**(실제 지시), 셋째 줄이 **후보열거**(대칭 나열)다. step4가 이 둘을 분리해 잰다.

#### 3.2 모델 — 4개

| 모델 | 종류 | 층 | Q헤드 | KV헤드 | 그룹 |
|---|---|---|---|---|---|
| Qwen2.5-Coder-3B-Instruct | 코드 특화 (기준) | 36 | 16 | 2 | 8 |
| DeepSeek-Coder-6.7B-Instruct | 코드 특화 | 32 | 32 | 32 | 1 |
| StableCode-Instruct-3B | 코드 특화 | 32 | 32 | 32 | 1 |
| Llama-3.2-3B-Instruct (unsloth 미러) | **범용** (대조) | 28 | 24 | 8 | 3 |

> 범용은 **1개뿐**이므로 일반화 근거가 아니다. **"코드 특화 3모델에서 일관, 범용 1모델은 달랐다"**로만 쓴다.

#### 3.3 측정 — 세 지표

```
준수율        새 함수 이름이 지침 표기를 따르는 비율            (생성)
선호 점수 S = logP(준수 후보) − logP(위반 후보)                (개입)
              후보 두 개 고정: removeDuplicates / remove_duplicates
              프롬프트 끝에 "def "를 붙여 이름이 올 자리를 만든다
되돌림 R    = (S_개입 − S_기준) / (S_깨끗 − S_기준)
              1 = 기준 상태까지 완전 회복, 0 = 변화 없음. 위가 막혀 있지 않다
```

`logP`는 후보 이름의 **토큰별 로그 확률 합**이다(곱셈이 덧셈이 되어 언더플로를 피한다).
뺄셈이 곧 **확률 비율의 로그**라, S의 부호가 어느 표기를 선호하는지를 그대로 나타낸다.
**단위는 nat** — S=1이면 약 2.7배 선호. 판정 불가 임계 1.0도 같은 단위다.
자세한 설명은 [`개념_트랜스포머와_개입.md`](개념_트랜스포머와_개입.md) §6.

> **본문에 반드시 넣을 단서 두 개**
> 1. **R은 위로 열려 있다** — 6.3의 "점수 ≠ 준수율" 논의가 여기서 시작된다.
> 2. **길이 정규화를 하지 않는다** — 두 후보의 조각 수가 다르므로(2 vs 3) S의 절대값은
>    모델 간 비교에 쓸 수 없다. 우리는 **같은 조건 안의 차이**만 쓴다.

#### 3.4 개입 규격 — 결론의 유효 범위가 여기서 정해진다

**이미 계산돼 캐시에 저장된 KV**(post-RoPE)의 특정 토큰 자리를 공여 값으로 덮는다.
가중치·FFN·임베딩은 건드리지 않는다.

| 무엇을 덮나 | 검증하는 가설 |
|---|---|
| **Key만** | 문제가 *어디를 보는가*(어텐션)에 있다 |
| **Value만** | 문제가 *무엇이 실려 오는가*(내용)에 있다 |
| **둘 다** | 결합 개입 (**상한이 아니다** — 실제로 Value 단독보다 작다) |

**평균 덮어쓰기(mean-pool)** — 같은 이름의 두 표기가 모델마다 다른 개수로 쪼개지므로
(`formatMatrix` 2조각 / `format_matrix` 3조각), 공여 조각들을 평균 내 대상 자리 전부에
덮는다. 토크나이저와 무관하고 건너뛰는 이름이 없다.

> ⚠️ **본문에 반드시 적을 것:** 모든 인과 결론은 **"post-RoPE KV 캐시를 평균 덮어쓰기로
> 바꾸는 규격"** 안에서의 결과다. native Key 경로 자체에 대한 주장이 아니다(§3-2).

#### 3.5 통제와 순효과 ← 이 절이 논문의 방법론적 무기다

> 덮어쓰기는 정보를 넣는 **동시에** 원래 표현을 지우고, 평균 풀링으로 단어 내부 구조도 뭉갠다.
> 그래서 통제 없이 잰 R은 **상한**이다.

| 스텝 | 처치 | 통제 | 통제가 잡는 것 |
|---|---|---|---|
| **step3** (코드) | 다른 camel 이름 | **다른 snake 이름** | 표기 정보 없이 덮기만 했을 때의 교란 |
| **step5** (지침) | 반대 지침 지시어 | **같은 지시어(self)** | 평균 풀링 + 덮어쓰기 교란 |
| | | 무관어(`project`) | 표기와 무관한 내용을 덮었을 때 |

**순효과 = 처치 − 통제.** 반드시 **같은 묶음끼리 먼저 빼고** 42묶음에 대해 평균·신뢰구간을 낸다
(묶음마다 이름이 달라 난이도가 다르다). 평균끼리 빼면 차이의 신뢰구간을 얻을 수 없다.

- step3: `scripts/step3_net_effect.py` — 권장 추정량 `다른camel − 다른snake`
- step5: `scripts/step5_net_effect.py` — 짝짓기 키 (모델, 묶음, 지침 방향)

#### 3.6 판정 불가

`|S_깨끗 − S_기준| < 1.0`이면 **문맥·지침이 애초에 행동을 흔들지 못한 조건**이므로 R을 해석하지 않는다.
분모가 잡음이면 비율이 의미를 잃는다. 임계 민감도(0.5 / 1.0 / 2.0)는 5장 각주.

#### 3.7 실제로 돌린 조건 — 전수 (표1 후보)

| 스텝 | n | 모델 | 문맥 위반 | 지침 방향 | 묶음 |
|---|---|---|---|---|---|
| step1 절벽 | 2,184 | **2** (Qwen·StableCode) | **0~12 전 구간** | camel·snake | 42 |
| step2 코드 관측 | 168 | 4 | 6/12 고정 | **camel만** | 42 |
| step3 코드 인과 | 504 | 4 | 6/12 고정 | **camel만** | 42 |
| step4 지침 관측 | 336 | 4 | 6/12 고정 | camel·snake | 42 |
| step5 지침 인과 | 336 | 4 | 6/12 고정 | camel·snake | 42 |
| step5 통제 | 672* | 4 | 6/12 고정 | camel·snake | 42 |
| step6 처방 | 2,184 | 4 | **12/12 (절벽 바닥)** | camel만 | 42 |
| step6 생성 검증 | 420 | 4 | 12/12 | camel만 | 21 |
| step6 층 대조 | 336 | 4 | 12/12 | camel만 | 21 |
| 진단 A | 72 | 4 | 6/12 | camel·snake | 6 |
| **합계(분석에 쓴 것)** | **7,212** | | | | |

**전 스텝 공통 고정:** 합성 코드 · POOL 구성 · **긍정형** · **약한 어조** · **시드 42** · **파이썬**

> \* **각주(본문에 달 것):** step5 통제는 `results/step5_instr-cause-control/`에 2,016개가 있으나,
> 분석에는 **Key·Value·K+V를 분해해 잰 672개**만 쓴다. 나머지 1,344개는 **K+V만 잰 초기 실행분**
> (단일 층 672 + 봉우리±3층 672)으로, 방식 분해가 없어 5.2의 비교에 쓸 수 없다.
> 결과는 불변 규약(§6)에 따라 지우지 않고 보존한다.
>
> 처치 336 ↔ 통제 672의 짝은 정확하다 — 처치 1조건마다 공여 2종(자기·무관어)이 대응한다.

**축 분해** — n이 스텝마다 다른 이유는 축이 다르기 때문이다(중복·누락 0):

| 스텝 | 계산 | 왜 그 축이 있나 |
|---|---|---|
| step2 | 4모델 × 42묶음 = 168 | 관측이라 **공여 축이 없다** |
| step3 | 4 × 42 × **공여 3** = 504 | 처치 + 통제 2종(무관 camel·무관 snake) |
| step4 | 4 × 42 × **방향 2** = 336 | 지침 양방향 |
| step5 | 4 × 42 × **방향 2** = 336 | 처치(반대 지침) 하나 |
| step5 통제 | 4 × 42 × 방향 2 × **공여 2** = 672 | 자기 통제 + 음성 통제 |

step3은 **공여**에, step5는 **방향**에 예산을 썼다 — 그래서 504 vs 336이다.

#### 3.8 고정한 것이 곧 한계다 (7장으로 넘어감)

| 고정한 축 | 못 하는 말 |
|---|---|
| 파이썬만 (JS 결과 **0개**) | "언어 무관하게 성립한다" |
| step2·3은 **camel 지침만** | step2의 "위반 구간을 더 본다"에서 **위반 ≠ snake를 분리 못 함**. step3의 인과도 snake→camel 한 방향 |
| 긍정형·약한 어조만 | 지침 표현 방식의 효과 |
| 문맥 위반 6/12 한 점(기제 실험) | 위반 비율 전 구간의 기제 (step1이 행동만 담당) |
| 이름 한 쌍으로 채점 | 다른 이름쌍으로의 일반화 |
| 범용 모델 1개 | 모델 종류 전반의 일반화 |

### 4장. 문맥 위반과 준수율 절벽 (RQ1) — 0.55쪽

**그림1** — 위반 개수(0~12) × 준수율, 지침 방향 2종.

| 주장 | 근거 |
|---|---|
| **문맥 위반은 준수율을 떨어뜨린다** | 위반 0→4에서 Qwen 1.00→0.55, StableCode 0.71→0.07 |
| **완만한 하락이 아니라 임계다** | **위반 5개에서 정확히 0.000**, 42묶음 전부. 그 아래로 회복 없음 |
| **단, 지침이 모델의 사전 선호와 어긋날 때만** | snake 지침이면 위반 12개에서도 0.88~1.00 (천장) |
| **지침 순응도와 붕괴 지점은 별개** | 위반 0에서 1.00 vs 0.71로 다른데 **붕괴 지점은 둘 다 5** |

> **정직하게 쓸 것:** snake 방향은 **판정 불가**(천장 포화)다. RQ1은 **방향 조건부**로 성립한다.
> 이걸 숨기면 리뷰어가 반드시 찾아낸다. 오히려 "사전 선호와 지침이 충돌할 때만 문맥이 힘을 갖는다"는
> **더 정확한 주장**으로 바꿔 쓰는 편이 강하다.

---

### 5장. 신호는 어디에 있는가 (RQ2·RQ3) — 1.65쪽 ★

#### 5.1 코드의 표기 신호 (RQ2) — 0.7쪽

관측(step2)은 **3문장으로 압축**한다: 네 모델 모두 위반(snake) 구간을 토큰당 더 참조하고,
격차 봉우리는 중후반 층이다. 다만 관측은 "더 봐서"인지 "내용이 커서"인지 못 가른다 → 인과로 넘어간다.

**그림2** — 층별 표기 순효과 곡선(Value vs Key), 대표 모델 1~2개.
**표2** — 봉우리 층에서의 순효과 4모델.

| 모델 | 봉우리 층(깊이) | Value | Key | K+V |
|---|---|---|---|---|
| Qwen2.5-Coder-3B | L25 (71%) | **0.253±0.008** | −0.005±0.001 | 0.117±0.006 |
| DeepSeek-Coder-6.7B | L20 (65%) | **0.125±0.008** | 0.004±0.002 | 0.055±0.005 |
| Llama-3.2-3B | L15 (56%) | **0.358±0.018** | 0.003±0.001 | 0.102±0.008 |
| StableCode-3B | L18 (58%) | **0.189±0.012** | 0.021±0.003 | 0.080±0.006 |

| 주장 | 근거 | 강도 |
|---|---|---|
| **신호를 나르는 것은 Value다** | 순효과 0.13~0.36, 4모델 모두 CI가 0을 넘지 않음 | 강 |
| **Key 개입의 추가 효과는 실용적으로 무시할 수준** | camel을 덮든 snake를 덮든 **원값이 같다**(0.374 vs 0.379). 순효과 −0.005~0.021 = Value의 2~11%. **CI는 0을 배제**하므로 "0"이라 쓰지 않는다 | 중강 |
| **후반 단일 층에 국소화** | 봉우리 절반 이상인 층이 모델마다 정확히 하나. 깊이 56~71% | 중강 |
| **옮겨지는 것은 이름이 아니라 표기 형태** | 같은 이름 준수판(0.536) ≈ 무관한 camel 이름(0.514) | 중 |

> **반드시 함께 쓸 것 — "어텐션은 잉여" 주장은 철회한다.**
> K+V(0.117)가 Value 단독(0.253)의 **절반 이하**다. 잉여라면 같아야 한다.
> 공여의 Key를 넣으면 그 자리를 참조하는 정도가 함께 바뀌어 제대로 넣은 내용이 덜 읽힌다
> → **어텐션과 내용은 독립적으로 더해지지 않고 상호작용한다.** 이건 약점이 아니라 **더 흥미로운 발견**이다.

#### 5.2 지침의 영향력 (RQ3) — 0.7쪽

**그림3** — 토큰당 참조량(지시어 vs 코드 이름) 층별 곡선 + **인과 봉우리 층 표시**.
지금 `docs/step4/figures/layer_alignment_*.png`가 이미 이 그림이다. **관측과 인과를 한 장에 겹치는 것이 이 논문에서 가장 설득력 있는 그림**이다.

**표3** — step5 처치·통제·순효과.

| 모델 | 층 | 방식 | 처치 | 자기 통제 | **순효과 ±95%(짝지음)** |
|---|---|---|---|---|---|
| Qwen | L27 | Value | 0.775 | 0.050 | **0.725±0.022** |
| | | Key | 0.106 | 0.107 | **−0.001±0.008** ← 0을 문다 |
| DeepSeek | L17 | Value | 0.417 | 0.087 | **0.330±0.009** |
| | | Key | 0.175 | 0.181 | **−0.006±0.004** (음수) |
| StableCode | L19 | Value | 0.887 | 0.093 | **0.793±0.104** |
| | | Key | 0.231 | 0.239 | **−0.008±0.006** (음수) |

| 주장 | 근거 | 강도 |
|---|---|---|
| **모델은 지침을 덜 보지 않는다** | 코드 특화 3모델에서 지시어가 코드 이름보다 **토큰당 1.55~2.46배**. 봉우리 층에서는 **10~20배** | 강 |
| **반대 지침 공여가 자기 풀링 교란을 넘지 못한다** | 짝지음 순효과 −0.008~−0.000 — Qwen·Llama는 **CI가 0을 물고**, DeepSeek·StableCode는 0을 배제하나 **음수**. 어느 쪽도 정보 전달의 근거가 아니다 | 강 |
| **지침도 Value 통로 — 코드와 같은 구조** | 순효과 0.33~0.79, 거품(자기통제) 0.05~0.09 | 강 |
| **지침을 보는 층 = 지침이 작동하는 층** | Qwen L27=L27, DeepSeek L17=L17, StableCode L19=L19 | 강 |
| **지침의 힘은 지시어 한 단어에 집중** | 그 단어만 무관어로 덮어도 전이 0.27~0.45 | 중 (여유 있으면) |

> **범용 모델은 분리해서 쓴다.** Llama는 168조건 중 **104개가 판정 불가**이고 순효과 0.014다.
> **"지침이 안 통한다"가 아니라 "지침 축 자체가 약하다"** — 이 구분을 지킨다.

#### 5.3 측정 타당성 (0.25쪽) — 리뷰어 방어

압축해서 4~5문장 + 각주.

| 검증 | 무엇을 막나 | 결과 |
|---|---|---|
| **진단 A (RoPE 노름)** | "평균 덮어쓰기가 Key만 쪼그라뜨린 것 아닌가" | **노름 붕괴 가설 기각.** Key 0.944~0.963 > Value 0.785~0.892 |
| **위치 어긋남** | "공여 위치와 대상 위치가 달라 위상이 어긋난 것 아닌가" | Qwen·Llama는 **0칸**(토큰 수가 같아 정확히 일치)인데도 Key 순효과가 없다 |
| (미검증) | post-RoPE Key의 **방향·위상 정합성** | **재지 않았다 → 한계로 명시** |
| 분할검증 | 봉우리 층이 우연 아닌가 | 앞 21묶음으로 고르고 뒤 21묶음에서 평가 — 4모델 모두 같은 층 |
| 임계 민감도 | 판정 불가 기준 1.0이 자의적 아닌가 | 0.5·1.0·2.0 어디서도 값이 안 바뀐다 |
| **측정 결정성** | "다시 돌리면 같은 값이 나오나" | step2와 step4는 **168개 조건이 겹친다**(같은 선행·시드·지침 방향). 서로 다른 시점의 별도 실행인데 **80,640개 값이 최대 차이 0.0으로 전부 일치**한다 |

> **결정성 검증은 각주 한 줄로 충분하지만 반드시 넣는다.** 관측 실험이 그리디·결정적이라는
> 주장을 데이터로 뒷받침하는 유일한 증거이고, 비용이 0이다(이미 있는 결과를 대조만 했다).

> **진단 A는 반드시 본문에 한 줄이라도 넣는다.** 편향의 방향이 우리 결론과 같아서,
> 안 다루면 리뷰어가 가장 먼저 찌를 지점이다.

---

### 6장. 처방 — 잔차를 밀 것인가, 어텐션을 키울 것인가 — 1.05쪽

**이 장의 논리적 지위를 첫 문단에서 못 박는다:**

> 5장이 옳다면 두 가지가 **예측**된다(따름정리가 아니라 예측이다). ① 그 층 부근의 표현을 밀면 준수가 되살아나야 한다.
> ② 지침 스팬의 **어텐션을 키우는 것은 효과가 없어야 한다.** 6장은 이 예측을 검증한다.

**6.1 두 처방** (0.3쪽)

| | 방법 | 근거 |
|---|---|---|
| **잔차 스트림 조향** (우리) | 그 층 **블록 출력(잔차)** 에 `세기 × (camel−snake 방향)` 더하기 | 5장에서 **동기를 얻은** 별도 가설(선형 방향). CAA·ITI 계열 |
| **Spotlight** (기존) | 전 층·전 헤드에서 지시어 스팬 어텐션 비중을 ψ까지 끌어올림 | 재구현 |

출발점은 **절벽 바닥**(위반 12/12, 준수율 0). 스팬은 **step5에서 값을 바꾼 자리와 정확히 같게** 잡는다
— 그래야 공정한 대결이다.

**6.2 결과** (0.45쪽) — **그림4**(세기 곡선 + Spotlight 최고선), **표4**

| 방법 | Qwen | DeepSeek | Llama | StableCode |
|---|---|---|---|---|
| 무개입 | 0.000 | 0.000 | 0.000 | 0.000 |
| 잔차 조향 세기2 | **1.643** | **1.920** | **1.385** | 2.006 |
| Spotlight 지시어 ψ0.3 | −0.125 | −0.051 | **0.904** | 0.274 |

| 주장 | 근거 |
|---|---|
| **어텐션을 키워도 준수는 안 돌아온다** | 회복 −0.37~0.27 (코드 특화 3모델) |
| **"구현 실패"가 아니다** | 지시어 비중 **0.002 → 0.231, 100배 이상**. 16개 조건 전부 상승 |
| **값을 밀면 되살아난다** | 실제 준수율 세기 1~2에서 Qwen 1.000 · DeepSeek 0.952 · Llama 1.000 |

**6.3 점수는 준수율을 대변하지 않는다** (0.3쪽) — **방법론적 기여**

| Qwen 세기 | 점수 회복 | **실제 준수율** | 나온 이름 |
|---|---|---|---|
| 1 | 1.243 | **1.000** | `removeDuplicates` |
| 2 | 1.643 | **1.000** | `removeDuplicates` |
| **4** | **1.733** (최고) | **0.000** | `RemoveDuplicates` ← PascalCase |

> 선호 점수는 위가 막혀 있지 않다. 세게 밀수록 계속 오르지만 그 지점에서 모델은
> 이미 다른 표기로 넘어가 있다. **회복률이 1을 넘는 구간은 되살아난 것이 아니라 지나친 것이다.**
> 조향 논문이 점수 회복만으로 성공을 주장하는 관행에 대한 경고다.

---

### 7장. 논의·한계·결론 (0.35쪽)

**정직하게 적을 한계 5가지** — 숨기지 않는 것이 이 논문의 자산이다.

1. **범용 모델 1개뿐.** "코드 특화 3모델에서 일관"까지만 말할 수 있다. Llama는 Spotlight가 작동(0.904)한다
2. **층 특이성은 처방 단계에서 성립하지 않는다.** 교차 주입이 더 높은 경우도 많다 → 5장의 국소화는 **관측·인과 층의 성질**이지 처방의 조건이 아니다
3. **이름 한 쌍·절벽 한 지점.** 세기 격자도 성기다(1·2·4·8)
4. **StableCode 크기는 보고하지 않는다.** 방향에 따라 3배 차이(0.45 vs 1.32) — 층·방식 대비만 쓴다
5. **자기증폭 미측정.** step1은 첫 함수만 잰다

---

## 3. 이 골격이 요구하는 것 — 이미 있는가?

| 필요한 것 | 상태 | 비고 |
|---|---|---|
| 그림1 절벽 | ✅ `docs/step1/figures/cliff.png` | |
| 그림2 층별 순효과 | ✅ `docs/step3/figures/code_causality_*.png` | 4장 중 1~2장만 선택 |
| 그림3 관측+인과 층 일치 | ✅ `docs/step4/figures/layer_alignment_*.png` | **이 논문 최고의 그림** |
| 그림4 세기 곡선 | ✅ `docs/step6/figures/strength_*.png` | |
| 표1~4 | ✅ 전부 results.md에 집계돼 있음 | 옮겨 적기만 |
| 진단 A 수치 | ✅ | 각주 1줄 |
| 분할검증 | ✅ step3 | |
| 임계 민감도 | ✅ step5 | |

**추가 실험은 필요 없다.** 6쪽에는 오히려 **가진 것의 절반도 못 넣는다.**

### 넣지 않기로 하는 것 (지면 부족)

| 잘라내는 것 | 이유 |
|---|---|
| step1 자바스크립트 | RQ1 방향 조건부 주장에 파이썬만으로 충분. 각주 1줄 |
| step2의 ‖v‖·v 코사인 | 관측은 3문장으로 압축 |
| step6 교차 주입(층 특이성) | 7장 한계 2번에 한 문장 |
| step5 지시어 절제 | 여유 있으면 5.2에 한 문장, 없으면 삭제 |
| 하네스 구조·조용한 실패 방지 | 논문 주제가 아니다. 재현성 각주에 저장소 링크만 |

---

## 3-2. ★ 주장 강도 규약 — 이 표현은 쓰지 않는다

외부 검토에서 **현재 실험이 입증한 범위를 넘는 문장** 세 종류가 지적됐고, 저장소에서
확인한 결과 **타당했다.** 본문에서 왼쪽 표현을 쓰지 않는다.

| ✗ 쓰지 않는다 | ✓ 이렇게 쓴다 | 왜 |
|---|---|---|
| "Key가 나르는 표기 정보는 **0**이다" | "Key 순효과는 Value의 **2~11%로 실용적으로 무시할 수준**" | 집계 스크립트가 `0을 무나: 아니오`로 찍는다 — **신뢰구간이 0을 배제**한다 |
| "RoPE 우려는 **기각됐다**" | "**노름 붕괴** 가설은 지지되지 않았다. 방향·위상은 검증하지 않았다" | 진단은 `‖mean(K)‖ / mean‖K‖`만 잰다 |
| "native Key 경로에 표기 정보가 없다" | "**이 평균 덮어쓰기 규격에서** Key 개입의 추가 효과가 작다" | post-RoPE 캐시를 평균으로 덮은 조건만 관측 |
| "지침을 더 보므로 문제는 attention이 아니다" | "'지침을 거의 안 본다'는 설명은 성립하지 않는다" (인과 근거는 **step5**) | 합·토큰당 평균은 **서로 다른 추정 대상** |
| "**값(Value) 조향**" | "**잔차 스트림 조향**(activation steering)" | 구현은 블록 출력 전체에 더한다 — 이후 층의 Q·K·V·FFN을 모두 바꾼다 |
| "step6은 step3이 찾은 Value 방향을 민다" | "step6은 step3에서 **동기를 얻은 별도 가설**(잔차 공간의 선형 방향)" | 두 가설은 다르다 |
| "자기 통제는 정보가 안 바뀌는 항등 개입" | "자기 통제는 **평균 풀링 + 덮어쓰기 교란**을 잡는다" | `mean` 규격이라 (k₁,k₂)→(평균,평균) |
| "후보열거는 **완전 대칭**" | "**역할 대칭**" | 토큰 수가 DeepSeek 3:4, StableCode 2:3 |
| "‖av‖ = 실제 기여량" | "Σa‖v‖ = **상한 대리치**(상쇄 무시·W_O 이전)" | `‖Σaᵢvᵢ‖`가 아니다 |

**논문 전체에 안전한 한 문장(초록·결론에 쓸 것):**

> 평균 덮어쓰기 KV 개입이라는 특정 규격에서, Value 개입은 일관된 표기 특이 효과를 보였고
> Key 개입의 추가 효과는 실용적으로 무시할 수준이었다. 다만 post-RoPE Key의 방향·위상
> 정합성은 완전히 검증되지 않았다. step6은 이 결과에서 동기를 얻은 **별도의 잔차 스트림
> 조향** 실험이다.

### 이 규약이 요구하는 추가 작업

| | 무엇 | 상태 |
|---|---|---|
| ① | step5 **순효과의 짝지음 신뢰구간** | **완료** — `scripts/step5_net_effect.py`. Key 순효과는 0 아니면 **음수** |
| ② | step3 순효과를 **짝맞춤(다른camel−다른snake)** 기준으로 통일해 보고 | 값은 이미 있음, 표만 정리 |
| ③ | 4장·5장에서 "합 기준"과 "토큰당 기준"을 **둘 다** 제시 | 값은 이미 있음 |

---

## 4. 예상 반론과 방어 배치

| 반론 | 어디서 막나 |
|---|---|
| "덮어쓰기 자체의 교란 아닌가" | **3.5 통제** + 표2·표3의 통제 열 |
| "Key는 RoPE 때문에 불리했던 것 아닌가" | **5.3 진단 A** — 단 노름만 답한다. 위상은 Qwen·Llama의 **위치 어긋남 0칸**으로 방어하고, 나머지는 한계로 적는다 |
| "Spotlight를 제대로 구현 못한 것 아닌가" | **6.2 ψ 전후 0.002→0.231** |
| "봉우리 층이 우연 아닌가" | **5.3 분할검증** |
| "점수 올랐다고 진짜 되는 건가" | **6.3 실제 생성 준수율** |
| "판정 불가 기준이 자의적" | **5.3 민감도** |
| "모델 4개로 일반화되나" | **7장 한계 1** — 범위를 좁혀 주장 |

---

## 5. 제목 가안

| | 국문 | 영문 |
|---|---|---|
| A | 코드 생성 모델은 왜 표기 규약을 어기는가: 어텐션이 아니라 내용에 있는 신호 | Why Code LLMs Break Naming Conventions: The Signal Lives in Values, Not Attention |
| B | 표기 규약 위반의 기제와 처방: 내용 통로에 대한 인과 분석 | Mechanism and Remedy of Naming-Convention Violation |
| C | 지침은 읽히지만 지켜지지 않는다: 표기 규약 준수 실패의 층위별 인과 분석 | Read but Not Followed |

**A 추천** — 반박 대상(어텐션)과 우리 주장(내용)이 제목에서 대비된다.

---

## 6. 집필 순서 (권장)

1. **3장 실험 설계** 먼저 — 정의를 고정해야 나머지가 안 흔들린다
2. **5장** — 심장부터. 여기서 쪽수가 확정돼야 나머지를 맞출 수 있다
3. **6장** → **4장** → **7장**
4. **1장 서론**은 **마지막에** — 본문이 실제로 무엇을 보였는지 확인한 뒤 써야 과장이 안 들어간다
5. 2장 관련 연구 · 요약 · 주제어

---

## 7. 연구자 결정 — ①②⑤ 확정

| | 질문 | **결정** |
|---|---|---|
| **①** | 제목 | **A 확정.** 국문 「코드 생성 모델은 왜 표기 규약을 어기는가」 · 영문 「Why Code LLMs Break Naming Conventions: The Signal Lives in Values, Not Attention」 (국문은 제목상자 5.5cm를 넘기지 않도록 부제를 뺐다) |
| **②** | 5장 그림2를 몇 모델 실을까 | **1개(Qwen만) 확정.** 나머지 3모델은 표2로. 아낀 약 0.15쪽은 5.2와 6.3에 준다 |
| **③** | step5 "지시어 한 단어" 결과 | **미정.** ②로 지면이 생겼으니 5.2에 한 문장(전이 0.27~0.45)으로 넣을 여유가 있다 |
| **④** | RQ1의 자바스크립트 | **미정.** 계획대로 각주 1줄 권장 — JS는 결과 **0개**이므로 "안 돌렸다"가 아니라 "설계상 스윕이 불가능했다"로 적어야 한다 |
| **⑤** | 6장에서 반박 대상 호칭 | **Spotlight 명시 확정.** 우리가 실제로 재구현한 것이 그것이고, 대결 구도가 선명해진다. 단 범용 모델에서는 Spotlight가 작동(0.904)한다는 사실을 같은 절에 붙여 공정하게 쓴다 |
| **⑥** | 발표 형태 | **미정.** 어느 쪽이든 6쪽 골격은 그대로다(구두는 3쪽 이상이 하한이라 제약이 안 걸린다) |

### LaTeX 골격 ↔ 이 문서의 대응

`paper/main.tex`가 위 결정을 반영해 만들어져 있다. 절마다 `%[계획]` 주석으로
**무엇을 쓰나 · 근거 스텝 · 무슨 주장 · 쪽수 예산**이 들어 있고, §3-2의 주장 강도
규약은 파일 맨 위에 "쓰지 않는 표현" 목록으로 박아 두었다.

| 이 문서 | `main.tex` | 그림·표 |
|---|---|---|
| 1장 | `\section{서론}` | — |
| 2장 | `\section{관련 연구}` | — |
| 3장 | `\section{실험 설계}` (`sec:setup`) | 표1 `tab:conditions` |
| 4장 | `\section{문맥 위반과 준수율 절벽}` (`sec:rq1`) | 그림1 `fig:cliff` |
| 5.1 | `\subsection{코드의 표기 신호}` (`sec:rq2`) | 그림2 `fig:codenet` · 표2 `tab:codenet` |
| 5.2 | `\subsection{지침의 영향력}` (`sec:rq3`) | 그림3 `fig:align` · 표3 `tab:instrnet` |
| 5.3 | `\subsection{측정 타당성}` (`sec:validity`) | — (각주) |
| 6장 | `\section{처방: 잔차를 밀 것인가...}` (`sec:remedy`) | 그림4 `fig:strength` · 표4 `tab:remedy` |
| 7장 | `\section{논의 및 한계}` (`sec:limit`) + `\section{결론}` | — |

> `paper/`는 블라인드 심사 때문에 저장소에 올리지 않는다(`1db8ac3`). 골격은 zip으로
> 전달했고 Overleaf에서 관리한다. **본문 산문이 어느 정도 차면 이 대응표만 갱신한다.**

### 표2·표3 수치 재검증 (이 골격을 만들며 확인)

`results/`를 pure Python으로 다시 집계해 이 문서의 표와 **소수 셋째 자리까지 전부 일치**함을 확인했다.

- 표2(step3 코드 순효과, `unrelated_camel − unrelated_snake`, 42묶음 짝지음):
  Qwen L25 value 0.253±0.008 / key −0.005±0.001 / K+V 0.117±0.006 — 4모델 전부 일치
- 표3(step5 지침 순효과, `처치 − 자기통제`, 판정불가 제외):
  Qwen L27 value +0.725±0.022 / key −0.001±0.008 — 4모델 전부 일치.
  Llama는 84조건 중 **34개만** 판정 가능(순효과 +0.014)

즉 표를 논문에 옮겨 적을 때 **다시 계산할 필요가 없다.**
