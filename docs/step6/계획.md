# step6 — 처방 (Prescription): 값 조향 vs Spotlight

> **브랜치 안내(2026-08).** 이 계획서는 `step6/steer` 브랜치에서 왔고, 지금은 통합 브랜치
> `integration/step1-5`를 기준으로 한다. 엔진은 `src/harness/steer.py`에 있고
> 노트북은 `notebooks/step6_steer.ipynb`다. 아래 §9의 설계는 그대로 살아 있으나,
> 옮겨 오면서 **조용히 틀리는 지점 여섯 곳을 고쳤다** — §10 참조.

**스텝:** step6 · **성격:** 처방(고치기) + **기존 기법 대비 비교** · **논문에서:** 헤드라인 결과(비교표)

> 논문 형식은 "방법 제안 + 기존 기법 대비 성능 비교"(HCLT 지식편집 논문 계열)를 따른다.
> step1 = 문제 제기(절벽), step2~5 = "왜 값이냐"의 근거(압축), **step6 = 결과 표(Spotlight vs 우리)**.

---

## 1. 왜 하나

step1~5 결과: 지침 위반의 **문제도 손잡이도 어텐션이 아니라 후반 특정 층의 Value(내용)**에 있다.
그 귀결을 **직접 손대서** 확인한다:

> **그 층 Value에 "camel 방향"을 밀면 절벽으로 무너진 준수가 되살아나고(우리 처방),
> 지침에 어텐션을 더 주는 기존 접근(Spotlight)은 안 된다.**

---

## 2. 기여 3불릿 (논문용)

- **(한계 지적)** 표기 규약 위반에서 **지침에 어텐션을 더 주는 접근(Spotlight류)이 효과 없음**을 지적한다.
- **(우리 것)** 손잡이가 어텐션이 아니라 **후반 특정 층의 Value**임을 규명하고, **그 층 Value를 미는 처방**을 제시한다. *(방법 발명이 아니라 "손잡이 규명 + 처방" — 조향 기법 자체는 CAA·ITI 인용.)*
- **(실증)** 동일 데이터·모델에서 **값 조향이 Spotlight보다 준수를 크게 회복**시킴을 실험으로 입증한다.

---

## 3. 무엇을 측정하나 (지표)

출발점 = step1의 **절벽 바닥**(camel 지침 + 앞 코드가 전부 snake 위반 → 준수 거의 0).
여기에 각 방법을 걸고 **얼마나 되살리나**를, 4모델 다 깨끗한 **교사강제**로 잰다.

| 지표 | 뜻 |
|---|---|
| **준수 회복** (주지표) | camel 선호 점수 회복, 0=회복없음 · 1=완전회복(오염없는 상태 기준) |
| camel 선호 점수 | camel로 쓸 확률 − snake로 쓸 확률 (교사강제) |
| 멀쩡한 이름 비율 | 너무 세게 밀어 이름이 깨지지 않았나 (자유생성, qwen·stable 보조) |
| **지시어 어텐션 비중** | Spotlight가 **실제로 작동했는지** 증명용(개입 전/후) → "구현 탓" 반박 차단 |

---

## 4. 비교 표 (헤드라인 = 그 논문의 "MEMIT vs Ours" 자리)

| 방법 | 준수 회복 | camel 선호점수 | 멀쩡한 이름% |
|---|---|---|---|
| 무개입 (지침만) | 0.00 | … | … |
| **Spotlight** (어텐션 키우기) | (낮음) | … | … |
| 값 조향 @ 엉뚱한 층 | (낮음) | … | … |
| **우리** (값 조향 @ 맞는 층) | **(높음)** | … | … |

*그림 = 조향 세기에 따른 회복 추이(방법별)* — 그 논문의 "그림3(n에 따른 성능)"과 같은 자리.

---

## 5. 우리 방식 (값 조향)

- **조향 방향:** step3에서 뽑은 **camel value − snake value** (그 층). 인과로 이미 검증됨. **모델별 자체 유도**(Llama 벡터 빌리는 거 아님).
- **주입:** 그 층 잔차에 `세기 × camel 방향` 더하기. 활성값 더하기 조향 **[CAA(ACL 2024), ITI(NeurIPS 2023)] 인용**.
- **세기 스윕:** 약→강 여러 값. 이름 깨지는 선 함께 관찰.

---

## 6. 비교 대상 (Spotlight 재구현) — 식 그대로

Spotlight(EACL 2026, arXiv:2505.12025)를 **원문 식(2)~(5)대로** 재구현. 그들 벤치마크 숫자를 가져오는 게 아니라 **우리 데이터에 다시 돌린다.**

**전 층·전 헤드**, 각 쿼리 위치·헤드에서:
1. 소프트맥스 → 지시어 스팬 비중 `ψ_current` 측정
2. **게이팅:** `ψ_current ≥ ψ_target`이면 개입 안 함
3. 아니면 지시어 로짓에 `log(ψ_target/ψ_current)` 더하고 **재소프트맥스** (언더슛 정상)

- **세기 = ψ_target** (논문 기본 0.1 + 스윕). eager 어텐션 위에서 구현.
- **최선을 준다**(세기 넉넉히) → 그래도 지면 반증이 강해짐.

**논문 미명시 → 우리가 정하고 명시할 3가지:**
| 자유도 | 우리 결정 |
|---|---|
| 프리필 적용 | 전 쿼리 위치 적용 |
| 헤드별 vs 평균 ψ | 헤드별 독립 |
| 스팬 S | 지침 지시문 전체(특수토큰 제외) — Spotlight에 최선 |

---

## 7. 대조군 요약 (같은 데이터·지표)

| 조건 | 무엇 | 예상 | 역할 |
|---|---|---|---|
| 무개입 | 절벽 그대로 | 바닥 | 하한선 |
| **값 조향 @맞는층** | camel 방향 밀기 | **회복 ↑** | 우리 주장 |
| 어텐션 키우기 | Spotlight 재구현 | 거의 X | 어텐션 접근 반증 |
| 값 조향 @엉뚱층 | 초반 층에 같은 방향 | 거의 X | 층 특이성 |

---

## 8. 고정 설정 (다른 스텝과 동일)

- 데이터셋 504·42묶음·시드 42·파이썬
- 층 = step3 후반 피크(qwen·deepseek·llama·stable 각각)
- **평균덮기는 KV group 단위**(§3 GQA)
- 결과는 **신뢰구간**까지 · 밀어도 안 움직이면 **판정불가** 분리

---

## 9. 엔진 (§7 — 새 스크립트 없이 값만 추가)

- `conditions.py`: `InterventionKind.VALUE_ADD` 추가. `Intervention`에 `strength`(세기), `steer_source="code_contrast"`. Spotlight는 기존 `ATTENTION_AMPLIFY` + `amplify`=ψ_target.
- `model.py`: ① 조향 방향 추출(step3 재사용) ② 값 더하기 forward 훅 ③ Spotlight 어텐션 훅(2-pass) — 셋 다 교사강제 점수 경로에 붙음.
- `runner.py`: 새 kind를 `_run_steer` 하나로 라우팅.
- 노트북은 조건만 바꿔 `run()` 호출 (§5·§7).

---

## 한 줄 요약

**절벽에서 값을 밀면 준수가 되살아나고(우리), 어텐션을 키우면 안 된다(Spotlight 반증) — 동일 데이터·지표로 나란히 비교한 표가 논문의 핵심 결과.**


---

## 10. 통합 브랜치로 옮기며 고친 것 (2026-08)

`step6/steer` 브랜치의 엔진을 그대로 쓰면 결과가 조용히 틀어지는 곳이 여섯 군데 있었다.

| # | 무엇 | 왜 위험한가 | 고친 방식 |
|---|---|---|---|
| **1** | **인과 마스크 누락** | 최신 transformers는 어텐션 함수에 `attention_mask=None`을 넘기고 인과성을 내부에서 처리한다. 우리가 어텐션을 다시 계산하면서 마스크를 안 씌우면 **모델이 미래 토큰까지 보게 된다.** 그 상태로 잰 "Spotlight는 효과 없음"은 Spotlight 탓이 아니라 우리가 모델을 망가뜨린 탓이다 | mask가 없으면 **직접 인과 마스크를 씌운다.** 소형 모델로 검증: 기본 구현과 오차 6e-08 |
| **2** | 개입이 안 걸려도 조용히 진행 | 어텐션 함수가 아예 안 불려도 "효과 없음"으로 저장된다 | 호출 횟수를 세고 **0이면 예외** |
| **3** | 개입 **전** 비중 미기록 | 비중이 실제로 올랐는지 못 보여주면 "구현을 못해서 진 것"이라는 반박을 막을 수 없다 | ψ를 **개입 전·후 모두** 기록 |
| **4** | 층 목록을 주면 **첫 층만** 사용 | step5 통제에서 실제로 터진 실수. 파일명엔 그 층만 남아 오해를 부른다 | 층이 둘 이상이면 **예외** |
| **5** | 조향 방향 추출의 조용한 skip | 이름을 못 찾은 표본을 그냥 건너뛰어, 몇 개로 만든 방향인지 알 수 없다 | 건너뛴 수를 세고 **절반 넘으면 예외**, 표본 수·노름을 결과에 기록 |
| **6** | Spotlight 스팬이 지시어 하나로 고정 | 스팬 선택이 결과를 좌우하는데 조건에 안 드러난다 | **조건 축으로 올림** — `span="rule_word"`(step5와 같은 자리) / `"instruction"`(원 논문 정의). 둘 다 돌린다 |

추가로 `gap`·`undecidable`을 결과에 저장해 판정 불가 조건을 사후에 가려낼 수 있게 했다.

### 검증

무작위 가중치 소형 Llama(다운로드 없음)로 전 경로를 돌렸다 — `tests/test_step6.py` 11개.

- 재가중이 원문 폐형 `ψ_after = ψ_target/(1+ψ_target−ψ_before)`과 일치, 게이팅 동작
- **인과성 보존** (ψ→0이면 기본 구현과 동일)
- 비중이 실제로 오르고 **언더슛**한다
- 훅이 지정 층에서만 걸리고, 해제 후 완전히 원복된다
- 세기에 단조롭게 반응한다
- 개입이 안 걸리면 예외, 빈 스팬은 만들 때 막힌다

### 조건 규모

모델당 546개 = 42묶음 × (무개입 1 + 값 조향 8 + Spotlight 4).
값 조향은 맞는 층·엉뚱한 층 × 세기 4종, Spotlight는 스팬 2종 × ψ 2종.
4모델 2184개 전부 파일명이 고유하며 기존 결과와 충돌하지 않는다.

### 아직 안 한 것

- **멀쩡한 이름 비율**(너무 세게 밀어 이름이 깨지지 않았나)은 생성 경로가 필요해 미구현이다.
  세기를 크게 밀 때 이 지표가 없으면 "점수만 올리고 이름은 망가뜨렸다"는 반론에 답할 수 없다.
  세기 스윕 결과를 보고 필요하면 추가한다.
