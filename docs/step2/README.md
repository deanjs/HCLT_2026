# step2 — 코드 신호 관측 (RQ2 관측)

> ⚠️ **이 스텝의 주요 결론은 철회됐다.** "모델이 문맥의 위반 이름을 더 참조한다 · 격차가 후반
> 층에서 커진다"는 **표기 배치가 시드로 고정된 데서 온 자리 효과**였다. 배치를 뒤집은
> 시드 67로 다시 재자 Qwen·Llama에서 부호가 뒤집혔다.
> → [`results.md`](results.md) **§3-6** · [`방법론.md`](방법론.md) **§6-2**
>
> 인과 스텝(step3·5·6)은 처치와 통제가 **같은 자리**를 건드리므로 이 교란의 영향을 받지 않는다.

| 읽을 순서 | 문서 | 무엇 |
|---|---|---|
| 1 | [`../개념_트랜스포머와_개입.md`](../개념_트랜스포머와_개입.md) | **개념 배경** — 트랜스포머 구조, 우리가 건드리는 곳, 측정 수식 |
| 2 | [`방법론.md`](방법론.md) | **이 스텝의 방법** — 왜 하나 · 무엇을 건드리나 · 수식 · 데이터 · 한계<br>**"대체 뭘 어떻게 잰 건가"가 안 잡히면 §2-1(프롬프트 원문)·§2-2(측정 절차)부터 본다** |
| 3 | [`../코드_하네스공통.md`](../코드_하네스공통.md) | **코드 배경** — 모듈 지도 · `run()` 라우팅 · HuggingFace/PyTorch |
| 4 | [`code.md`](code.md) | **이 스텝의 코드** — 개념↔코드 대응 · 호출 사슬 · 핵심 코드 · JSON 필드 |
| 5 | [`results.md`](results.md) | **결과와 해석** — §1 결과 JSON 읽는 법(필드 정의) · 표·해석 · 이해용/논문용 그림 |

## 폴더 안

```
방법론.md      방법 — 왜·무엇을·수식·데이터·한계
code.md        코드 — 개념↔코드 · 파이프라인 · 핵심 코드 · 공부거리
results.md     결과 — JSON 읽는 법(필드 정의)·표·해석·그림
구조적_결함.md      (참고) 당시 감사·계획 기록
figures/       그림 (explain_* = 이해용 · 나머지 = 논문용, pdf+png)
```

결과 원본: `results/step2_code-observe/` **334개** — **불변**(CLAUDE.md §6).
집계·작도는 `scripts/`의 스크립트로만 한다.

| 슬러그 끝 | 무엇 | 개수 |
|---|---|---|
| `__s42` | 기준 실행분. §3-1~3-5 그림이 이걸로 그려졌다 | 168 |
| `__s67` | **자리 통제** — 표기 배치를 뒤집은 여집합. §3-6 | 166 (2개 실행 사고로 유실) |

| 스크립트 | 만드는 것 |
|---|---|
| `scripts/step2_figures.py` | `spans_*` · `attention_*` · `av_*` · `vnorm_*` · `gap_*` (시드 42만) |
| `scripts/step2_token_bars.py` | 토큰별 어텐션 막대 + 자리 교란 검산 (시드 42만) |
| **`scripts/step2_position_control.py`** | **`position_control` · `explain_position_control` (두 시드)** |
