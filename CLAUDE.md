# CLAUDE.md

코드 생성 모델의 표기 규약 준수 실패에 대한 Probing 연구 저장소.

---

## §0. 전체 실험 순서 (처음부터 새로 구축)

예비 저장소의 결과(A1·A2)를 인용만 하지 않는다. **이 하네스에서 A1·A2를 직접 재현·생성한 뒤** 나머지 Step으로 진행한다. 순서는 다음과 같다.

| 단계 | 실험 | 무엇을 만드는가 | RQ | 계획서 위치 |
|---|---|---|---|---|
| **Step 0** | 하네스 골격 | 조건 스키마 고정 + 단일 진입점(선행코드/지침/개입/층/모델 축) | 전체 | §3 하네스 구조 |
| **Step A** | RQ1 절벽 재현 | 준수 4/3/2/1/0 배정 → 첫 함수 표기 측정, 자기증폭 확인 | RQ1 | 계획서 §3 RQ1 (A1 생성) |
| **Step B** | RQ2 기반 관측 | 지침 camel/snake 2×2 어텐션, 충돌 표기 참조, ‖av‖/‖v‖ | RQ2 | 계획서 §3 RQ2 (A2 생성) |
| **Step C** | RQ2 기반 개입 | 선행코드 Key/Value 치환 회복 + 무관 코드 통제 | RQ2 | 계획서 §3 RQ2 (A2 생성) |
| **Step 1** | 층 스윕 + K/V 분해 | 전 층 × (Key만/Value만/둘 다) + v 코사인 유사도 궤적 | RQ2 | 계획서 §5 Step 1 |
| **Step 2** | 모델 다양성 | 다른 패밀리 2~3개 반복 (피크 층·K/V 비율 보존 확인) | RQ2 | 계획서 §5 Step 2 |
| **Step 3** | 선행코드 구성 ablation | 클론 vs 개별 과제 | RQ1 | 계획서 §5 Step 3 |
| **Step 4** | 실제 저장소 코드 | Python/JS·Java × 지침 2×2 | RQ1 | 계획서 §5 Step 4 |
| **Step 5** | 부정형 지침 행동 | 2×2×2, 작은·큰 모델 병행 | RQ3 | 계획서 §5 Step 5 |
| **Step 6** | 부정형 지침 내부 | 지침 구간 토큰별 어텐션 분해 | RQ3 | 계획서 §5 Step 6 |
| **Step 7** | 방법론 구현·평가 | 형태 인식 선택적 스티어링 | 방법론 | 계획서 §5 Step 7 |
| **Step 8/9** | 로짓 렌즈 / 문헌·데이터셋 | 해석·집필 병행 | 선택 | 계획서 §5 Step 8·9 |

**임계 경로:**

```
Step 0(하네스) → Step A → Step B → Step C → Step 1 → Step 2 → Step 5 → Step 6 → Step 7
```

**병렬 가능:** Step 3, Step 4 (임계 경로와 독립)
**후순위:** Step 8, Step 9

계획서 원본은 `docs/연구계획서.md`.

---

## 작업별 참조 위치

| 하려는 작업 | 읽을 절 |
|---|---|
| 실험 착수 | §1 승인 절차 → §2 브랜치 → §5 노트북 |
| 코드 추가·수정 | §3 하네스 구조 |
| 커밋·푸시 | §2 브랜치 |
| 결과 해석·문서화 | §4 연구 구조 |
| 전체 실험 순서 확인 | §0 전체 실험 순서 |
| 전체 설계 확인 | `docs/연구계획서.md` |

---

## §1. 승인 절차 (최우선 규칙)

**실험을 먼저 실행하지 않는다.** 순서는 항상 다음과 같다.

1. **방법론을 먼저 설명한다** — 무엇을 왜 측정하는지, 조건을 어떻게 나누는지, 무엇이 나오면 어떤 해석이 되는지
2. **승인을 받는다**
3. **그 다음 실행한다**

설명은 **짧고 쉽게** 쓴다. 연구자가 실험 내용을 완전히 숙지한 상태에서 승인해야 하므로:

- 전문 용어를 쓸 때는 한 줄로 풀어 쓴다
- 수식보다 "무엇을 바꾸고 무엇을 재는가"를 먼저 말한다
- 결과가 어느 쪽으로 나오든 각각 무슨 뜻인지 미리 밝힌다
- 길게 쓰지 않는다. 승인 판단에 필요한 것만

승인 없이 실행된 실험은 결과와 무관하게 폐기한다.

---

## §2. 브랜치와 푸시

**main에 직접 push 금지.** 예외 없다.

**실험 하나당 브랜치 하나.** 명명 규칙:

```
Step<번호>/<실험조건>
```

예시:
```
Step1/layer-sweep-kv-split
Step1/cosine-trajectory
Step2/deepseek-coder-1.3b
Step3/prefix-ablation-distinct-tasks
Step5/negation-2x2x2
```

**금지:**
- `claude/...`, `claude-code/...` 등 도구 이름이 들어간 브랜치
- 여러 실험을 한 브랜치에 몰아넣기
- 브랜치 없이 작업

로컬 동기화는 연구자가 `git pull`로만 수행한다.

---

## §3. 하네스 구조

**RQ별로 스크립트를 나누지 않는다.** RQ1·RQ2·RQ3은 같은 파이프라인에 조건 축만 다르다.

조건 축:

```
선행 코드   : 준수 개수 / 구성(클론·개별과제) / 출처(합성·실제저장소)
지침       : 형식(긍정·부정) / 목표 표기(camel·snake)
개입       : 없음 / Key / Value / Key+Value / 어텐션 증폭
층         : 전 층 스윕
모델       : 패밀리별
```

**규칙:**

- 조건 스키마를 먼저 고정하고, 단일 진입점에서 조건 조합을 받는다
- 새 실험은 스크립트 추가가 아니라 **조건 축 확장**으로 구현한다
- `run_rq1.py`, `rq2_attention.py` 같은 RQ 종속 파일명 금지
- 같은 로직을 두 곳에 복사하지 않는다

**측정 지표:** 준수율(생성), 준수 선호 점수(개입), 어텐션 가중치, ‖v‖, ‖av‖, v 코사인 유사도.

**GQA 주의:** Key/Value는 여러 query head가 공유하므로 치환은 KV group 단위로 수행한다. 모델마다 group 구성이 다르므로 매번 확인한다.

---

## §4. 연구 구조

RQ는 순차적으로 답을 찾아간다. 각 RQ는 독립 실험이 아니라 다음 RQ의 근거가 된다.

| | 질문 | 답이 무엇으로 이어지는가 |
|---|---|---|
| **RQ1** | 선행 코드의 위반이 준수율을 낮추는가 | → 무엇이 매개하는가 |
| **RQ2** | 매개 신호가 내용인가 형태인가, 어느 층인가 | → 지침 쪽도 같은가 |
| **RQ3** | 같은 원리가 지침 구간에도 적용되는가 | → 방법론 설계 |

**작업 시 지킬 것:**

- 실험 결과를 보고할 때는 **어느 RQ의 어떤 답에 해당하는지** 명시한다
- 결과 나열로 끝내지 않고 **해석**을 붙인다. 다음 RQ로 어떻게 이어지는지까지
- 방법론(형태 인식 선택적 스티어링)의 모든 설계 결정은 RQ의 답에 근거해야 한다. 답에 없는 아이디어를 끌어오지 않는다
- 예상과 다른 결과가 나와도 조건을 바꿔 맞추지 않는다. 그대로 기록한다

---

## §5. Colab 노트북

**실험마다 Colab용 주피터 노트북을 생성한다.** 모든 노트북은 동일한 형태를 따른다.

파일명: `notebooks/Step<번호>_<실험조건>.ipynb`

셀 구성 (순서 고정):

| # | 내용 |
|---|---|
| 1 | 실험 식별 — 어느 Step의 어떤 실험인지, 대응하는 RQ, 무엇을 확인하는지 |
| 2 | 환경 설정 — 설치, GPU 확인, 시드 고정 |
| 3 | 저장소 클론 및 브랜치 체크아웃 |
| 4 | 조건 설정 — 이 실험이 사용하는 조건 축 값 |
| 5 | 실행 |
| 6 | 결과 저장 — `results/` 하위, 조건이 파일명에 드러나게 |
| 7 | 요약 출력 — 표 또는 그래프 |

**주의:**

- 노트북에 실험 로직을 직접 쓰지 않는다. 저장소 코드를 호출만 한다
- 무료 티어 기준으로 작성한다. 메모리 초과 가능성이 있으면 셀 1에 미리 적는다
- 양자화를 쓰는 경우 표현 치환 정밀도 검증을 먼저 수행한다

---

## §6. 결과 기록

- `results/` 하위 산출물은 **불변**으로 취급한다. 덮어쓰지 않는다
- 실행 전 예측을 기록한 경우 결과와 함께 보존한다
- 예비 실험 저장소(`instruction-adherence`)는 설계 근거로 참조만 하고 수정하지 않는다
