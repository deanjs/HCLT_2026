# CLAUDE.md

코드 생성 모델의 **표기 규약 준수 실패**(camelCase/snake_case)를 파고드는 Probing 연구.
이 파일은 **통일 재실험** 기준으로 작성됐다. 이전(파일럿·1차 확대) 실험은 모두 `archive_v1/`에 보존한다.

- **전체 설계·실험 순서:** `docs/연구계획_통일재실험.md` (마스터 플랜)
- **이전 실험 아카이브:** `archive_v1/` (읽기 전용, 참고만)

---

## 작업별 참조 위치

| 하려는 작업 | 읽을 곳 |
|---|---|
| 전체 실험 순서·RQ 확인 | `docs/연구계획_통일재실험.md` |
| 실험 착수 | §1 승인 → §2 브랜치 → §5 노트북 |
| 고정 설정 확인 | §3 |
| 커밋·푸시 | §2 |
| 결과 해석·문서화 | §4 · §6 |
| 옛날 실험 참고 | `archive_v1/` |

---

## §1. 승인 절차 (최우선 규칙)

**실험을 먼저 실행하지 않는다.** 순서는 항상:

1. **방법론을 먼저 설명한다** — 무엇을 왜 측정하는지, 조건을 어떻게 나누는지, 어떤 결과가 무슨 뜻인지
2. **승인을 받는다**
3. **그 다음 실행한다**

설명은 **짧고 쉽게**. 전문 용어·줄임말을 쓰지 않고 직관적으로. 결과가 어느 쪽으로 나오든 각각 무슨 뜻인지 미리 밝힌다. 승인 없이 실행된 실험은 결과와 무관하게 폐기한다.

---

## §2. 브랜치와 푸시

**main 직접 push는 연구자가 지시했을 때만.** (문서 정리·아카이브 등 연구자가 "main에서 하고 push"라고 명시한 작업은 예외.)

**어시스턴트는 브랜치를 스스로 만들지 않는다.** 필요하면 **명령어만 제시**하고 연구자가 만든다.

**실험 하나 = 브랜치 하나.** 통일 재실험 브랜치(고정):

| 브랜치 | 스텝 | RQ |
|---|---|---|
| `step1/cliff` | 준수율 절벽 | RQ1 |
| `step2/code-observe` | 코드 관측 | RQ2 |
| `step3/code-cause` | 코드 인과 | RQ2 |
| `step4/instr-observe` | 지침 관측 | RQ3 |
| `step5/instr-cause` | 지침 인과 | RQ3 |

**금지:** 도구 이름 브랜치(`claude/...`), 여러 실험 한 브랜치에 몰기, 어시스턴트 임의 브랜치 생성.

---

## §3. 고정 설정 (모든 실험 공통)

| 축 | 값 |
|---|---|
| **모델 4개** | Qwen2.5-Coder-3B(기준) · deepseek-coder-6.7b · Llama-3.2-3B(unsloth 미러) · stable-code-3b |
| **데이터** | 함수 이름 504개(동사50×명사50), 12개씩 42묶음 커버 |
| **무작위 고정값(seed)** | **42** |
| **내부 값 치환** | **평균 덮어쓰기(mean-pool)** — 반대쪽 조각들을 평균 내 바꿀 단어 전 자리에 덮음. 토크나이저 무관, 코드·지침 동일 |
| **측정 지표** | 준수율 / 어텐션 / 영향력(지침·문맥이 행동을 실제로 바꾸는 정도, 0이면 제외) / 되돌림 정도(0~1) / 어느 층 |
| **언어** | 기제 실험(step2~5)은 파이썬. **step1(생성)만 파이썬+자바스크립트** 2종 |

세부 근거·프롬프트는 `docs/연구계획_통일재실험.md`.

---

## §4. 연구 구조 (RQ)

**큰 질문:** 코드 생성 LLM은 표기 규약 지침을 왜 자주 어기는가?
**주장:** 지침을 "덜 봐서"가 아니다. 문제도 손잡이도 **어텐션(얼마나 보나)이 아니라 내용(Value)**에, **후반 특정 층**에 있다. → 어텐션을 키우는 기존 접근(Spotlight) 반박.

| | 질문 | 스텝 |
|---|---|---|
| **RQ1** | 문맥의 규약 위반이 준수율을 떨어뜨리나 | step1 |
| **RQ2** | 코드 신호가 내용인가 형태인가, 어디·어느 층인가 | step2(관측)·step3(인과) |
| **RQ3** | 지침의 영향력도 같은 통로(내용/Value)인가 | step4(관측)·step5(인과) |

**실행 순서(GPU 가벼운 것부터):** step2 → step4 → step1 → step3 → step5.

**작업 규칙:**
- 결과 보고 시 **어느 스텝·어느 RQ**인지 명시하고, 나열이 아니라 **해석**을 붙인다
- 예상과 다른 결과도 조건 바꿔 맞추지 않고 **그대로 기록**한다
- 영향력이 0에 가까운 조건(지침/문맥이 안 먹히는 모델)은 "판정 불가"로 분리한다

---

## §5. Colab 노트북 규칙

**실험마다 노트북 1개.** 파일명: `notebooks/<스텝>_<조건>.ipynb`.

셀 구성(고정): ① 실험 식별(스텝·RQ·무엇 확인) ② 환경·시드42 ③ 저장소·브랜치 체크아웃 ④ 조건 설정 ⑤ 실행 ⑥ 결과 저장 ⑦ 요약·다운로드.

**필수 규칙:**
1. **모델 하나씩.** 노트북 위에서 모델 1개 고른다 → 런타임 끊겨도 그 모델만 영향
2. **재개.** 조건마다 저장, 이미 있으면 건너뜀 → 끊긴 뒤 남은 것만 이어서
3. **중간 결과 표시.** 진행·지금까지 값을 중간중간 출력
4. **쉬운 말.** 화면 출력에 줄임말·전문용어 금지 (예: "바꾼 조각 수", "영향력", "되돌림 정도")
5. **마지막에 zip 다운로드.** 결과 폴더를 zip으로 묶어 내려받는 셀
6. 노트북에 실험 로직을 쓰지 않는다 — **엔진(`src/harness`) 호출만**

무료 티어(T4) 기준. 메모리 초과 가능성은 셀 ①에 미리 적는다.

---

## §6. 결과 기록

- `results/` 산출물은 **불변**. 덮어쓰지 않는다
- 실행 전 예측을 기록했으면 결과와 함께 보존
- 실험 끝나면 해석 + 그래프를 `docs/<스텝>/결과.md`로 정리 (그래프 글자 안 깨지게 영문 라벨)
- `archive_v1/`(이전 실험)은 **읽기 전용** — 수정·삭제하지 않는다

---

## §7. 하네스(엔진) 구조

**RQ별로 스크립트를 나누지 않는다.** 모든 실험이 `src/harness`의 단일 진입점을 조건값만 바꿔 호출한다.

- 조건 스키마(`conditions.py`)에 값을 추가하는 방식으로 확장. `run_rq1.py` 같은 RQ 종속 파일명 금지
- **평균 덮어쓰기(mean-pool)는 KV group 단위**로 수행(§3 GQA 주의). 모델마다 group 구성이 다르니 매번 확인
